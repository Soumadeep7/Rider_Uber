<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ========================= -->
    <!-- RIDER APP BASE THEME -->
    <!-- ========================= -->
    <style name="Theme.RiderUber" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">#000000</item>
        <item name="colorPrimaryVariant">#000000</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <item name="colorSecondary">#3F51B5</item>
        <item name="colorSecondaryVariant">#303F9F</item>
        <item name="colorOnSecondary">@android:color/white</item>

        <item name="android:statusBarColor">#000000</item>
        <item name="android:windowLightStatusBar">false</item>

        <item name="android:windowFullscreen">false</item>
    </style>

    <!-- ========================= -->
    <!-- SPLASH SCREEN THEME -->
    <!-- ========================= -->
    <style name="Theme.RiderUber.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- ========================= -->
    <!-- FIREBASE LOGIN THEME -->
    <!-- ========================= -->
    <style name="Theme.RiderUber.Login" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">#3F51B5</item>
        <item name="colorPrimaryVariant">#303F9F</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <item name="android:statusBarColor">#3F51B5</item>
        <item name="android:windowLightStatusBar">false</item>

        <item name="android:windowBackground">@android:color/white</item>

        <item name="colorButtonNormal">#EBECF2</item>
    </style>

    <!-- ========================= -->
    <!-- FULLSCREEN DIALOG -->
    <!-- ========================= -->
    <style name="Theme.RiderUber.Dialog" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="Theme.RiderUber.AppBarOverlay" parent="ThemeOverlay.Material3.Dark.ActionBar" />

    <style name="Theme.RiderUber.PopupOverlay" parent="ThemeOverlay.Material3.Light" />

</resources>
